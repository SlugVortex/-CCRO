# Demo Rerecord Cue Sheet

Aligned to:

- `D:\My Downloads\ElevenLabs_2026-06-14T19_29_48_Roger - Laid-Back, Casual, Resonant_pre_sp100_s50_sb75_se0_b_m2.mp3`

Narration runtime:

- `2:52`

Use this while re-recording the screen.

## Setup before recording

- Open the app on `http://localhost:5173/dashboard`
- Make sure the API is running
- Set browser zoom to `90%` or `100%`
- Use light mode unless you specifically want the darker cinematic look
- Let the dashboard fully load before pressing play on the voice track

## Recording flow with cues

### 0:00 - 0:12

Script:

`Caribbean Climate Resilience Orchestrator is a Microsoft-native planning workspace for Caribbean climate adaptation.`

What to do:

- Start on the `Risk Map Dashboard`
- Do not move yet
- Let the viewer take in the page and branding

### 0:12 - 0:24

Script:

`It helps resilience teams identify where combined storm, flood, and sea-level stress is most concentrated.`

What to do:

- Move the cursor gently over the top metrics
- End with the cursor near the planning lens panel

### 0:24 - 0:38

Script:

`The first screen is the risk map dashboard. Here the planner sets the country, the time horizon, and the active hazard mix before running the risk lens.`

What to do:

- Click into `Country` briefly if needed, but keep `Jamaica`
- Click into `Time Horizon`, keep `2050`
- Hover across the three hazard buttons

### 0:38 - 0:52

Script:

`Once the lens is applied, the atlas highlights the highest-risk parishes and updates the detail panel with risk score, exposed population, facilities at risk, annual loss, and the core drivers behind that score.`

What to do:

- Click `Run Risk Lens`
- Wait for the refresh
- Move the cursor to the atlas detail panel on the right

### 0:52 - 1:08

Script:

`As the user clicks across different parishes, the model keeps the reasoning grounded at regional level, making it easy to compare hotspots and explain why one area is more exposed than another.`

What to do:

- Click `Kingston`
- Click `St. Catherine`
- Click `Portland`
- Pause half a second on the final selected parish

### 1:08 - 1:20

Script:

`From the dashboard, we can open the explorer for a larger operational view.`

What to do:

- Click `Open Explorer`
- Let the page transition complete

### 1:20 - 1:34

Script:

`This step expands the atlas into a focused inspection surface for live review and ministerial briefing.`

What to do:

- In explorer, click one atlas dot
- Then click one row in the `Region Ranking` table

### 1:34 - 1:48

Script:

`Next, the workflow moves from diagnosis into intervention planning. In the scenario builder, the user assembles an adaptation package and keeps it inside a budget ceiling.`

What to do:

- Navigate to `Scenario Builder`
- Pause at the top of the page
- Move the cursor to the budget slider

### 1:48 - 2:03

Script:

`The planner can combine actions like hardening hospitals, restoring natural buffers, reinforcing emergency access, and expanding shelters.`

What to do:

- Scroll slightly through the intervention cards
- Select 3 or 4 interventions if they are not already active

### 2:03 - 2:15

Script:

`The system is not just showing risk. It is testing policy choices under resource constraints.`

What to do:

- Click `Simulate Scenario`
- Wait for the scenario results to appear

### 2:15 - 2:29

Script:

`The recommendations view then turns the modeled result into a ranked action plan, tied to cost, protected population, targeted regions, and expected leverage.`

What to do:

- Go to `Recommendations`
- Hold on the table header
- Scroll slowly through the first few recommendations

### 2:29 - 2:42

Script:

`This is where the product becomes especially useful for decision makers, because the output is structured as a policy brief rather than a raw analytics screen.`

What to do:

- Keep scrolling slowly
- Pause on the brief or grounding section if visible

### 2:42 - 2:52

Script:

`Built around Microsoft Foundry, Azure OpenAI, Azure AI Search, and Azure Maps, Caribbean Climate Resilience Orchestrator is designed to help Caribbean institutions turn climate risk into prioritized action before the next storm arrives.`

What to do:

- Switch to `Data & Audit`
- Pause on the service status cards and tables
- Hold the final frame still until the audio ends

## Best final take path

If you want the cleanest possible retake:

1. Dashboard opening
2. Run Risk Lens
3. Click three parish dots
4. Open Explorer
5. Click one explorer dot and one ranking row
6. Open Scenario Builder
7. Simulate Scenario
8. Open Recommendations
9. Slow scroll
10. End on Data & Audit
